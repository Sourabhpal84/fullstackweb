import type { Metadata } from "next"; import Link from "next/link";
import { pizzas, commonFaqs } from "@/lib/content"; import { pageMetadata } from "@/lib/seo"; import { JsonLd } from "@/components/seo/json-ld"; import { faqSchema } from "@/lib/schema"; import { FaqList, PublicPage } from "@/components/seo/public-page";
export const metadata: Metadata = pageMetadata({ path: "/menu", title: "Vegetarian Pizza Menu Greater Noida | MAGNEETOZ", description: "Browse MAGNEETOZ vegetarian pizzas, paneer pizzas and Indian flavour pizzas with sizes and prices.", priority: .9, changeFrequency: "daily" });
export default function MenuPage(){return <PublicPage><JsonLd data={faqSchema(commonFaqs)}/><h1 className="text-4xl font-black">Vegetarian Pizza Menu in Greater Noida</h1><p className="mt-4 max-w-3xl leading-7 text-white/70">Explore MAGNEETOZ pizzas with readable ingredients, sizes and current guide prices. Confirm live availability and final pricing in the ordering menu.</p><section className="mt-10 grid gap-4 sm:grid-cols-2" aria-labelledby="pizza-menu"><h2 id="pizza-menu" className="col-span-full text-2xl font-black">Pizza categories and favourites</h2>{pizzas.map(p=><article key={p.slug} className="rounded-3xl border border-white/10 bg-white/[.06] p-6"><h3 className="text-xl font-black"><Link href={`/pizza/${p.slug}`}>{p.name}</Link></h3><p className="mt-2 text-white/65">{p.description}</p><p className="mt-3 font-bold">From ₹{p.prices[0].price} · Vegetarian</p></article>)}</section><section className="mt-12"><h2 className="mb-5 text-2xl font-black">Menu FAQs</h2><FaqList faqs={commonFaqs}/></section></PublicPage>}
